import Money from '../../app/scripts/tools/Money.js';
const should = require('chai').should();

const money = new Money();

describe('Money', () => {

	describe('roll', () => {

		it('should return a number', () => {
			//COSTS ALWAYS IN CP
		});
	});
});

